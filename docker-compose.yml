version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
#     depends_on:
#       - db
#     networks:
#       - app-network

#   db:
#     image: postgres:13
#     environment:
#       POSTGRES_USER: admin
#       POSTGRES_PASSWORD: admin
#       POSTGRES_DB: go_demo_db
#     ports:
#       - "5432:5432"
#     volumes:
#       - db_data:/var/lib/postgresql/data
#       - ./init.sql:/docker-entrypoint-initdb.d/init.sql
#     networks:
#       - app-network

# volumes:
#   db_data:

# networks:
#   app-network:
